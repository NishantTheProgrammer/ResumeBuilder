const Education = (classes) => (
    <form className={classes.form}>
        <div className={classes.formItem}>
            <label>Education component</label>
        </div>
    </form>
);

export default Education;