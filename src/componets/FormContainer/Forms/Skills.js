const Skills = (classes) => (
    <form className={classes.form}>
        <div className={classes.formItem}>
            <label>Skills component</label>
        </div>
    </form>
);

export default Skills;